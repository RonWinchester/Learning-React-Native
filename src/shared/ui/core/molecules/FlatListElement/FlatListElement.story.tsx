import { storiesOf } from '@storybook/react-native';
import { FlatListElement } from '.';

storiesOf('ui/molecules', module).add('flat-list-element', () => {
    return <FlatListElement item={''}
    imageSize={40}
    name={''}
    handler={()=>{}}/>
  });