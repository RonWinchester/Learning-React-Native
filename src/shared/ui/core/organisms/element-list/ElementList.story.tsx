import { storiesOf } from '@storybook/react-native';
import { ElementList } from '.';

/* TODO: не работает navigate внутри компонента - поправить 
storiesOf('ui/organisms', module).add('Element-List', () => {
    return (
        <ElementList items={[]} name={''} imageSize={0} goPage={''}/>
    );
  }); */